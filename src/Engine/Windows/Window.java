package Engine.Windows;

public interface Window {

    /**
     * Puts the objects in the correct position and gives them the correct size.
     * @param field The background which everything is based on
     */
    void setLocation(Backfield field);

}
